<UserControl
    x:Class="PeerConnectionClient.Controls.ErrorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PeerConnectionClient.Win10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="32"
    d:DesignWidth="400">

    <UserControl.Resources>
        <local:BoolToVisConverter Negated="True"  x:Key="BoolToVisConverterNegated" />
    </UserControl.Resources>
    <Grid>
        <ContentPresenter x:Name="MyPresenter" />
        <Button x:Name="LeftButton"
                Width="32"
                Height="32"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                IsTabStop="False"
                Opacity="1"
                Style="{StaticResource TextBlockButtonStyle}">
            <Button.RenderTransform>
                <CompositeTransform TranslateX="-25" TranslateY="0" />
            </Button.RenderTransform>
            <Button.Flyout>
                <Flyout Placement="Right">
                    <ItemsControl ItemsSource="{Binding Errors}" />
                </Flyout>
            </Button.Flyout>
            <Ellipse Width="10" Height="10"
               Fill="Red"
               Visibility="{Binding Valid, Converter={StaticResource BoolToVisConverterNegated}}"
               Stretch="UniformToFill" />
        </Button>
    </Grid>
</UserControl>
